#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 *
 * @a: inpute character to be multiplied
 * @b: input character to be multiplied
 * Return: Always 0.
 */

int mul(int a, int b)
{
	return (a * b);
}
