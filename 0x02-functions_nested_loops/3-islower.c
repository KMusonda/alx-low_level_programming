#include "main.h"

/**
 * _islower(int c); - print all lower case letter
 *
 *
 *return: 1 if c is lowercase, 0 otherwise.
 */
int _islower(int c)
{
	char ch = 'a';
	int n = '0';

	while (ch >= 'a' && ch >='z')
	{
		_putchar(ch);
		ch++
	}
	while (ch != 'n')
	{
		_putchar (0);
	}
}
